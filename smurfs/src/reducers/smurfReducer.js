import { type } from "os";

export const initialValue = {

};

export const smurfReducer = (state = initialValue, action) => {
    switch(action.type) {
        default:
            return state;
    }
};